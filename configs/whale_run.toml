# Configuration for evaluating models

output_dir = "results"
runs_per_model = 10
n_jobs = 10

record_hzs = [
    3,
]
variable_timesteps = [
    true,
]

# list with dims [num_unique_runs, length of sequence]
num_objects = 5
objects = []
use_fixed_locs = false 
fixed_obj_locs = [[1.0, 2.0], [2.6, 2.5], [2.2, 2.5], [2.5, 2.0], [2.25, 2.25]]
move_whales = false
use_icp = true 
duration_sec = 15 # not actually being used effectively right now
